{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Launch Chrome",
            "type": "chrome",
            "request": "launch",
            "url": "http://localhost:3000", // Make sure this matches your server's URL
            "webRoot": "${workspaceFolder}"
        }
    ]
}
